# macOS 本地冒烟环境（环境名：task2）
name: task2
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - torch==2.10.0
      - torchvision==0.25.0
      - -r requirements-local.txt
